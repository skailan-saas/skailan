
"use client";

export default function FlowsPage() {
  return (
    <div className="h-[calc(100vh-4rem)] flex flex-col items-center justify-center bg-muted/30 p-4">
      <h1 className="text-2xl font-semibold">Flows Page</h1>
      <p className="text-muted-foreground mt-2">Content is temporarily minimal to debug a parsing error.</p>
    </div>
  );
}
