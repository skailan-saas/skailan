# Database Connection
DATABASE_URL="postgresql://username:password@localhost:5432/skailan"

# Authentication & Security
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"

# AI Services (Optional)
OPENAI_API_KEY="your-openai-api-key"
GEMINI_API_KEY="your-gemini-api-key"

# Note: WhatsApp Business API credentials are managed per tenant in the database
# Configure them in Settings > Channels for each organization